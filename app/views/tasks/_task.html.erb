<tr>
	<td><%= task.content %></td>
	<td><%= link_to 'Show', task %></td>
	<td><%= link_to 'Edit', edit_task_path(task) %></td>
	<td><%= link_to 'Destroy', task, method: :delete, data: { confirm: 'Are you sure?' } %></td>
	<% if task.state == 1 %>
		<td><%= link_to 'Mark as todo', change_task_path(task, state: 0), method: :put  %></td>
		<td><%= link_to 'Mark as done', change_task_path(task, state: 2), method: :put  %></td>
	<% else %>
		<td><%= link_to 'Mark as doing', change_task_path(task, state: 1), method: :put  %></td>
	<% end %>
</tr>
